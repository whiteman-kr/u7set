#include "OutputController.h"

OutputController::OutputController(ITuningSignalManager* signalManager, ITuningConnection* tuningConnection, QWidget* parent):
	VFrame30::TuningController(signalManager, tuningConnection, parent)
{

}
