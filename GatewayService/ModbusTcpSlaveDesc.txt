// This is a Comment
// 
// [Gateway], [SignalList], etc - sections
//
// Type = V, ID = N, etc - settings
//
// Sections names, settings names are case insensitive
//
// Settings values are case sensitive
//
// * - required settings

[Gateway]   

GatewayType = ModbusTcpSlave            	// * gateway type
GatewayID = KAES_5SUZR1				// * gateway identifier
GatewayDescription = KAES block 5 SUZR 1	//   free style gateway description	

// ModbusTcpSlave gateway specifis settings

LocalGatewayIP1 = 192.160.101.100:502		// * IP and port of Modbus TCP server 1
// LocalGatewayIP2

RemoteGatewayIP1 = 192.160.101.1:502		// * local IP and port for connect Modbus TCP server 1
// RemoteGatewayIP2


CodingMode = ASCII				// ASCII - two ASCII characters on each 8-bit byte
						// RTU - two 4-bit HEX char on each 8-bit byte (binary format)
ModbusDeviceAddress = 20				

[SignalList]

// float16 - analog format float16
// be - Big Endian
// le - Little Endian                                       

SignalsFormat = AnalogFloat16 BE

100 -> #SIGNAL1					// regAddr = AppSignalID in list default format
101 -> SIGNAL2			     		// regAddr = CustomSignalID with register specific format

[SignalList]

SignalsFormat = DiscreteUint16 LE		// general discrete registers format, Big Endian 16 bit

100, 3 -> #SIGNAL3				// regAddr, bitNo = AppSignalID in list default format
100, (0x8000) -> SIGNAL4			// regAddr, (mask) = CustomAppSignalID in list default format
100, 3 -> #SIGNAL3				// regAddr, bitNo = AppSignalID with specific format
